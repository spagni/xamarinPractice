<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HelloWorldPCL.InstaLayout" Padding="0, 20, 0, 0">
    <StackLayout>
        <StackLayout Padding="10">
            <Label Text="sebastian.pagni" FontSize="Medium"/>
        </StackLayout>
        <Image Source="http://lorempixel.com/1920/1080/nature/3/"/>
        <StackLayout Orientation="Horizontal" Spacing="20" Padding="10, 0">
            <Button Text="Like" TextColor="LightBlue" BackgroundColor="Transparent"></Button>
            <Button Text="Comment" TextColor="LightBlue" BackgroundColor="Transparent"></Button>
            <Button Text="Share" TextColor="LightBlue" BackgroundColor="Transparent"></Button>
        </StackLayout>
        <StackLayout Padding="10">
            <BoxView HeightRequest="1" Color="#f0f0f0" />
            <Label Text="700 likes" FontAttributes="Bold" />
            <Label TextColor="#444" Text="This is a shot yesterday in Skanor, Sweden, when driving home, from a couple of days of work in Denmark." />
        </StackLayout>
    </StackLayout>
</ContentPage>